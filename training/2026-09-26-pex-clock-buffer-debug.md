# PEX 后 16G 时钟链路异常：Debug 面试训练

日期：2026-09-26
模式：Debug 面试／RX clocking + PEX awareness
状态：等待主问题回答

## 主问题

在 16 GHz RX clock chain 中，前仿真时 LDO 输出只有轻微纹波；PEX 后把完整 clock chain、LDO 和负载一起仿真，发现运放仍能钳位，但 LDO 输出出现明显高频尖峰和大幅纹波。

请按“现象→假设→实验→预期结果→排除/保留→根因”建立 debug tree，至少覆盖三类假设：

1. LDO 环路本身不稳定；
2. 时钟负载的周期性动态电流激励 LDO；
3. PEX 后电源/地寄生、供电走线或耦合形成新反馈路径。

至少提出 3 个最小代价对比实验，并说明要观察的节点和指标。